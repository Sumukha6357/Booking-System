spring:
  application:
    name: platform-api

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/booking_db}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:root}

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

  flyway:
    enabled: false
    baseline-on-migrate: false
    validate-on-migrate: true
    locations: classpath:db/migration

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true

server:
  port: 8080

app:
  env: ${APP_ENV:local}
  jwt:
    secret: ${JWT_SECRET:titanium_booking_secure_key_32_chars_at_least}
    expiry-seconds: ${JWT_EXPIRY_SECONDS:7200}
    refresh-expiry-seconds: ${JWT_REFRESH_EXPIRY_SECONDS:1209600}
  webhook:
    secret: ${WEBHOOK_SECRET:dev_webhook_secret}
  cors:
    allowed-origin: ${CORS_ALLOWED_ORIGIN:http://localhost:3000}

integrations:
  mode: ${INTEGRATIONS_MODE:mock}
  payment:
    real:
      api-key: ${PAYMENT_REAL_API_KEY:}
  email:
    real:
      api-key: ${EMAIL_REAL_API_KEY:}
  sms:
    real:
      api-key: ${SMS_REAL_API_KEY:}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha

logging:
  pattern:
    console: '{"ts":"%d{yyyy-MM-dd''T''HH:mm:ss.SSSX}","level":"%p","corr":"%X{correlationId:-}","tenant":"%X{tenantId:-}","logger":"%c{1}","msg":"%m"}%n'
